# Dynamic configuration — middlewares, routers, services not managed by Docker labels
http:
  middlewares:
    platform-auth:
      forwardAuth:
        address: "http://backend:8000/auth/verify"
        authResponseHeaders:
          - "X-User-Id"
          - "X-User-Role"
